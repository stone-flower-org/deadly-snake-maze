import { styled } from '@mui/material';

import { Canvas } from '@/src/modules/common/ui/components/Canvas';

export const StyledContainer = styled('div')`
  height: 100%;
  width: 100%;
  overflow: hidden;
`;

export const StyledCanvas = styled(Canvas)``;
